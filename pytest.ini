[pytest]
# 1. Enable live logging directly in the terminal
log_cli = true
log_cli_level = INFO
log_cli_format = %(asctime)s [%(levelname)s] %(message)s

# 2. (Optional) Automatically stream all test logs to a file
log_file = log/test_run.log
log_file_level = DEBUG
